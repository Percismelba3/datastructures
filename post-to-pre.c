#include<stdio.h>
#include<conio.h>
int getpreceed(char ch)
{
                if(ch=='*'&&ch=='/')
                    return(2);
                if(ch=='+'&&ch=='-')
                    return(1);
}
char pop(char stack[],int *top)
{
                char ch=stack[*top];
                *top=*top-1;
                return(ch);
}
void push(int *stack,int val,int *top)
{
                int num=stack[*top];
}
void main()
{
int stack[],postfix[],ch;
                char post[]="ab+c-d/e";
                postfix[]={!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!};
                stack[]={$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$};
                getpreceed(char ch);
                if(ch="a"||"b"||)
                                push(stack[]);
                if(ch='+')
                                push(postfix[]);
                push()

}

